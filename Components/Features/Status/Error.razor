@page "/Error"
@using System.Diagnostics

<PageTitle>Error | Samet Can Cıncık</PageTitle>

<HeadContent>
    <meta name="description" content="An unexpected error occurred while processing your request." />
</HeadContent>

<div class="page-container page-section flow-lg">
    <BackToHome />

    <div class="flow-sm">
        <h1 class="text-page-title">Error.</h1>
        <p class="text-body text-muted">An error occurred while processing your request.</p>
    </div>

    @if (ShowRequestId)
    {
        <div class="card card-soft flow-sm">
            <p class="text-body"><strong>Request ID:</strong> <code>@RequestId</code></p>
        </div>
    }

    <section class="flow-sm">
        <h2 class="text-section-title">Development Mode</h2>
        <p class="text-body text-muted">
            Swapping to <strong>Development</strong> environment will display more detailed information about the error that occurred.
        </p>
        <p class="text-body text-muted">
            <strong>The Development environment shouldn't be enabled for deployed applications.</strong>
            It can result in displaying sensitive information from exceptions to end users.
            For local debugging, enable the <strong>Development</strong> environment by setting the <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong>
            and restarting the app.
        </p>
    </section>
</div>

@code{
    private string? RequestId { get; set; }
    private bool ShowRequestId => !string.IsNullOrEmpty(RequestId);

    protected override void OnInitialized() =>
    RequestId = Activity.Current?.Id;
}
