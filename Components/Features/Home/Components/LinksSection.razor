@* LinksSection.razor - "Connect With Me" social links section *@

@inject ISocialLinkProvider SocialLinks
@{
    var visible = SocialLinks.Links
        .OrderBy(l => SocialLinks.GetCategoryOrder(l.Category))
        .ThenBy(l => l.Label)
        .ToList();
}

@if (visible.Count > 0)
{

}
