# Required for scripts/generate-content-images.ts
OPENROUTER_API_KEY=

# Optional: overrides the placeholder model constants in the script.
# Text model: reads content and writes the image-generation prompt.
OPENROUTER_TEXT_MODEL=
# Image model: generates the cover image from that prompt.
OPENROUTER_IMAGE_MODEL=
